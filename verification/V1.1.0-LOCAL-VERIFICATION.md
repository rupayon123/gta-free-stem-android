# Android v1.1.0 local release-candidate verification

> **SUPERSEDED HISTORICAL SNAPSHOT — NOT CURRENT RELEASE EVIDENCE**
>
> This file preserves the August 19, 2026 source hashes, signed AAB/APK
> identities, test counts, runtime checks, and screenshot evidence. The app
> source has materially changed since then, so none of those results verifies
> the current working tree or a current release candidate. A current candidate
> requires a source freeze, clean rebuild, full retest/device verification,
> fresh signed artifacts, screenshot recapture, and regenerated provenance.
> No upload, rollout, or publication is authorized by this record.

Evidence was recorded on August 19, 2026 for the executable app source snapshot
identified below. It does not claim evidence for today's source, a Play upload,
Play review, physical-device pass, production approval, or public availability.

## Historical candidate identity

```text
Base commit: bb67cbbcaf5dda20c8f6408768faa64d3ab51b2f
Historical source state: modified/untracked working tree; final reviewed commit pending
app/src/main aggregate SHA-256: b405953bcdab3fb5ee4c3ec1430b326697907a4f79fd3f324375a040c45c2448
Package: com.rupayonhaldar.gtafreestem
Version: 1.1.0 (versionCode 3)
minSdk / targetSdk: 26 / 36
Catalog SHA-256: a5f8eee787db084843d2cf9a5bb337187fa4d0e75c7832c3dd0079b13671457d
Bundled feed SHA-256: 2da9c4dadbce14feb16b3306d366b48ef0535febd0d9cfee314266d3e3fd211f
```

Because that historical tree was not a final reviewed commit, the signed
artifacts below were local release candidates, not authorized upload artifacts.
Freeze and commit the current reviewed tree, then rebuild and repeat the
identity/hash checks.

## Recorded August 19 automated verification

- Keychain-backed `REQUIRE_SIGNING=1` preflight: 0 failures.
- JVM: 151 tests across 26 suites, 0 failures/errors/skips; an uncached forced
  execution also passed.
- Feed-copy audit: all 82 publisher excerpts at the upstream 260-unit boundary
  either end at a complete sentence or render as a faithful word-boundary
  prefix with an ellipsis; none is blank, malformed Unicode, or over the bound.
- Debug lint: 0 errors, 23 reviewed non-blocking warnings.
- Release lint: 0 errors, 23 reviewed non-blocking warnings.
- API 36 medium-phone instrumentation with gesture navigation: 19 of 19.
- API 36, 900 dp tablet override: all four MainActivity adaptive/navigation/RTL
  tests passed.
- API 36 phone, three-button navigation: the system-navigation inset regression
  passed.
- `git diff --check`: passed.

The reviewed lint warnings are dependency/update notices, obsolete-SDK checks,
unused resources, and KTX style suggestions; no lint error was suppressed to
obtain the result.

## Historical signed artifacts — do not upload

```text
Signed AAB: app-release.aab
Size: 5,983,283 bytes
SHA-256: 0e59dc75edafe0c8a432f4462dc3d974d105c9c92e44b138374cf78ef3c67176

Signed APK: app-release.apk
Size: 3,286,717 bytes
SHA-256: 30713d26f70c941d868741d32188e5644cd322c1034596c1c3b6e3aadac546c7

Upload-certificate SHA-256:
0D:8E:B2:CD:06:A1:89:B5:FF:7C:15:2A:B7:AE:72:C5:60:89:26:4C:FD:D5:64:95:E2:71:5B:BC:A7:DB:F7:B5
```

- `jarsigner` verified the AAB; the only strict warning is the expected
  self-signed upload-certificate chain.
- `apksigner` verified one RSA-4096 APK signer and a valid v2 signature.
- AAB and APK DEX hashes match. All four native payload hashes match.
- R8 mapping SHA-256 is
  `a0e07e2a2b1128cd46e9abfe3de942aefa9e68b139378eec2acd68aaea17ea8e`;
  the AAB-embedded map matches, and `OpportunityAlertWorker` plus its
  `(Context, WorkerParameters)` constructor remain retained.
- The signed APK passes `zipalign -P 16`. Every native ELF LOAD segment is
  `2**14`, and the AndroidX graphics-path libraries are already stripped.
- No executable app/build input was newer than the signed AAB.

## Recorded August 19 release runtime smoke

The signed APK installed on the API 36 emulator as v1.1.0/code 3. A normal cold
launch completed, a warm `gtafreestem://opportunities` link opened the
Opportunities UI, and a cold `gtafreestem://high-school` link opened the High
school hunt engine. Dynamic shortcuts, notification-channel/work scheduling,
and cold/warm deep-link behavior are also covered by instrumentation.

## Recorded August 19 manifest, privacy, and dependency review

- Source permissions: Internet, approximate location, and notifications.
- Location is a user-triggered one-shot coarse foreground fix held in memory;
  no precise/background permission, persistence, or transmission is used.
- Alerts are opt-in local WorkManager checks; there is no remote push provider.
- Backup/device transfer and cleartext traffic are disabled.
- Only MainActivity is app-exported; merged library exports are protected by
  their required system permissions.
- No ads, analytics, billing, Firebase, crash uploader, attribution SDK, paid
  map SDK, map tiles, or account service is present in the release dependency
  inventory.

## Historical store visual evidence

Six August 19 exact-to-snapshot debug screenshots are 1080 x 1920 RGB PNGs. The
icon is 512 x 512 RGBA and the feature graphic is 1024 x 500 RGB. Full historical
source/APK/image hashes and capture state are in
`play-store/assets/PLAY_ASSET_PROVENANCE.txt`. These images document only the
captured historical debug visuals, not today's source, a current signed AAB, or
a Play-generated install.

## Gates still open

1. Review/freeze/commit the current working tree; rebuild and independently
   reverify new signed artifacts so their hashes are attributable to that
   commit, then recapture all current visual evidence.
2. Configure a monitored support email, deploy the prepared Android-aware
   privacy/support/terms update, and verify those pages while logged out.
3. Complete truthful live Play Data safety, content-rating, target-audience,
   Families, app-access, ads, and policy declarations for the exact artifact.
4. Test the signed/Play-generated build on representative physical hardware,
   including API 26 behavior, install/upgrade, accessibility, and notification
   flows.
5. Review Play processing, supported-device results, the pre-launch report, and
   every Console warning before an explicitly authorized track upload or
   rollout.

No upload, publication, commit, push, or deployment was performed while
recording this evidence.
